return {
  "echasnovski/mini.surround",
  lazy = false,
  opts = {
    mappings = {
      add = "gs", -- Add surrounding in Visual mode
      delete = "ds", -- Delete surrounding
      find = "sf", -- Find surrounding
      replace = "cs", -- Change surrounding
    },
  },
}
